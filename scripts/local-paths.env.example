# Copy to scripts/local-paths.env and adjust if your folders differ.
# scripts/local-paths.env is gitignored.

# Vitality vision runtime (directory containing vision_api_server.py and model assets)
VITALITY_RUNTIME_DIR="/Users/ConstiX/Downloads/vitality_food_demo_frozen_20260409/runtime"

# Nutrition review API fallback (directory containing api_server.py — Unsloth / FastAPI).
# When REVIEW_MODE=full, start-local-stack.sh uses ./api_server.py in the repo root if present,
# otherwise this directory.
NUTRITION_REVIEW_DIR="/Users/ConstiX/Downloads/consumer prompt engineering"

# Optional: explicit Python interpreters
# VISION_PYTHON="/Users/ConstiX/Downloads/vitality_food_demo_frozen_20260409/.venv311arm2/bin/python"
# REVIEW_PYTHON="/path/to/unsloth/venv/bin/python"

# mps = Qwen2.5 + PyTorch MPS (default). stub = placeholder. full = Unsloth (CUDA).
# REVIEW_MODE="mps"
